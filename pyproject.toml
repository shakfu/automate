[project]
name = "automate"
version = "0.3.0"
description = "Reusable CI/CD workflows and release helpers for Python projects"
readme = "README.md"
requires-python = ">=3.13"
license = "MIT"
authors = [
    { name = "Shakeeb Alireza", email = "shakfu@users.noreply.github.com" },
]
keywords = ["ci", "cd", "release", "changelog", "github-actions", "pypi"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Topic :: Software Development :: Build Tools",
    "Programming Language :: Python :: 3.13",
]
dependencies = [
    "click>=8.5.0",
]

[project.urls]
Homepage = "https://github.com/shakfu/automate"
Repository = "https://github.com/shakfu/automate"
Issues = "https://github.com/shakfu/automate/issues"

[project.scripts]
automate = "automate.cli:cli"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-cov>=6.0",
    "ruff>=0.16.6",
    "mypy>=2.3.1",
    "pyyaml>=6.0.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/automate"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"

[tool.coverage.run]
source = ["automate"]
# __main__.py is exercised by tests/test_cli.py::TestModuleEntryPoint, which
# runs `python -m automate` in a subprocess; coverage cannot see into it.
omit = ["*/automate/__main__.py"]

[tool.mypy]
python_version = "3.13"
strict = true

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
